version: "3"
services:
    my-app:
        image: webpack
        build:
            context: .
            dockerfile: Dockerfile.dev
        links:
            - postgres
            - mongo
        env_file: .env
        environment:
            - DATABASE_POSTGRES_URL=$DATABASE_POSTGRES_URL
            - DATABASE_MONGO_URL=$DATABASE_MONGO_URL
            - PORT=9001
        ports:
            - "9001:9001"
        restart: always
        volumes:
            - ./:/dist:rw
            - node_modules:/dist/node_modules
    postgres:
        container_name: postgres
        image: postgres:12
        ports:
            - "5432:5432"
        volumes:
            - ./pg-data:/var/lib/postgresql
            - ./server/db/postgres/migrations/dbinit.sql:/docker-entrypoint-initdb.d/dbinit.sql
        restart: always
        env_file: .env
        environment:
            - POSTGRES_USER=$POSTGRES_USER
            - POSTGRES_PASSWORD=$POSTGRES_PASSWORD
            - POSTGRES_DB=$POSTGRES_DB
    pgadmin:
        image: dpage/pgadmin4:4.18
        restart: always
        env_file: .env
        environment:
            - PGADMIN_DEFAULT_EMAIL=$PGADMIN_DEFAULT_EMAIL
            - PGADMIN_DEFAULT_PASSWORD=$PGADMIN_DEFAULT_PASSWORD
            - PGADMIN_LISTEN_PORT=5001
        ports:
            - "5001:5001"
        volumes:
            - pgadmin-data:/var/lib/pgadmin
        depends_on:
            - postgres
    mongo:
        image: mongo
        ports:
            - "27017:27017"
        restart: always
        volumes:
            - mongo-data:/data/db
    mongo-express:
        image: mongo-express
        ports:
            - "8081:8081"
        restart: always
        depends_on:
            - mongo
        environment:
            - ME_CONFIG_MONGODB_PORT=27017
volumes:
    pg-data:
    pgadmin-data:
    mongo-data:
    node_modules:
